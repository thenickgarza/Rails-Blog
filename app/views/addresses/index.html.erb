<p style="color: green"><%= notice %></p>

<h1>Addresses</h1>

<div id="addresses">
  <% @addresses.each do |address| %>
    <%= render address %>
    <p>
      <%= link_to "Show this address", person_address_path(@person, address) %>
    </p>
  <% end %>
</div>

<%= link_to "New address", new_person_address_path %>
